# Recall@k Metric Configuration
#
# Measures the proportion of relevant documents retrieved.
# recall@k = (relevant docs in top-k) / (total relevant docs)
#
# Parameters:
#   k: Cutoff for evaluation (default: 10)
#
_target_: autorag_research.metrics.retrieval.recall.RecallConfig
k: 10
