# Precision@k Metric Configuration
#
# Measures the proportion of retrieved documents that are relevant.
# precision@k = (relevant docs in top-k) / k
#
# Parameters:
#   k: Cutoff for evaluation (default: 10)
#
_target_: autorag_research.metrics.retrieval.precision.PrecisionConfig
description: "Precision@k - Retrieval precision"
k: 10
